# Iterations

Iteration history for the FinTrack Pro pipeline run.

Each iteration folder contains the logs and results produced during that cycle. The pipeline may run multiple iterations if the scoring thresholds are not met; this sample run completed in a single iteration.

## Contents

| Directory | Description |
|-----------|-------------|
| `iteration-1/` | First (and only) iteration -- all phases completed successfully |
