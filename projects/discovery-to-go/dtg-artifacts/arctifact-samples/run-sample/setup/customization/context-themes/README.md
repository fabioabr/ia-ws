# Context Themes

Context theme definitions that can be used to customize the discovery pipeline for different product categories.

This directory is empty in the sample run. The active context is defined in `current-context/` instead. Add theme files here to provide reusable context presets across multiple runs.
