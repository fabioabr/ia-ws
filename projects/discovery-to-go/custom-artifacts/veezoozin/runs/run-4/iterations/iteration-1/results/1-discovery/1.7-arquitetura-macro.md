---
title: "Bloco 1.7 — Arquitetura Macro"
block: "1.7"
owner: solution-architect
project-name: veezoozin
iteration: 1
generated-by: solution-architect
generated-at: 2026-04-12 12:10
status: completo
---

# Bloco 1.7 — Arquitetura Macro

> **Especialista responsável:** Solution Architect
> **Modo:** Consultor ativo — arquitetura, trade-offs e recomendações
> **Contexto anterior:** Blocos 1.1-1.6 — SaaS NL-to-SQL, equipe de 1, GCP serverless, 4 personas, BigQuery read-only, BYOK, LGPD profundo (pseudonimização de PII requerida, DPAs necessários, testes de isolamento obrigatórios)

---

## 1. Dados Coletados

### 7.1 Padrão Arquitetural

**Recomendação: Monolito Modular em 2 processos (frontend + backend API)**

| # | Pergunta | Resposta | Tag |
|---|----------|----------|-----|
| 1 | Padrão? | [INFERENCE] Monolito modular: 1 serviço Next.js (frontend + BFF) + 1 serviço FastAPI (backend API + NL-to-SQL engine). Dois Cloud Run services, mesmo repositório (monorepo). Justificativa: com 1 dev, microsserviços são antipattern. Monolito modular dá isolamento lógico sem overhead de infra. | [INFERENCE] |
| 2 | Por que não microsserviços? | [INFERENCE] Antipattern #6 do SaaS blueprint: "microsserviços antes de validar produto". Com 1 dev, o custo de manter inter-service communication, deployment de múltiplos serviços e debugging distribuído não compensa. Monolito modular com bounded contexts é a decisão certa para MVP. | [INFERENCE] |
| 3 | Separação frontend/backend? | [INFERENCE] Dois Cloud Run services: (1) `veezoozin-web` — Next.js 14, SSR, serve UI; (2) `veezoozin-api` — FastAPI, NL-to-SQL engine, integração BigQuery/LLM. Justificativa: separação permite escalar backend independentemente (queries pesadas) sem afetar frontend. | [INFERENCE] |

**Diagrama de Arquitetura Macro:**

```
┌─────────────────────────────────────────────────────────────────┐
│                        INTERNET (HTTPS)                          │
└───────────────┬──────────────────────┬──────────────────────────┘
                │                      │
    ┌───────────▼───────────┐  ┌──────▼──────────────────────┐
    │   veezoozin-web       │  │   veezoozin-api             │
    │   (Cloud Run)         │  │   (Cloud Run)               │
    │                       │  │                              │
    │   Next.js 14          │  │   FastAPI                    │
    │   - UI/Dashboard      │──│   - REST API                │
    │   - SSR/RSC           │  │   - Auth middleware          │
    │   - BFF (API routes)  │  │   - Tenant isolation         │
    │                       │  │   - NL-to-SQL Engine         │
    └───────────────────────┘  │   - Query Validator          │
                               │   - Chart Generator          │
                               └──────┬───────────────────────┘
                                      │
                   ┌──────────────────┼──────────────────────┐
                   │                  │                      │
          ┌────────▼──────┐  ┌───────▼───────┐  ┌──────────▼─────┐
          │  Cloud SQL    │  │  Firestore    │  │  Secret Mgr    │
          │  (PostgreSQL) │  │               │  │                │
          │  - Tenants    │  │  - Sessões    │  │  - API Keys    │
          │  - Users      │  │  - Cache      │  │  - BQ Creds    │
          │  - Schemas    │  │  - Histórico  │  │  - Stripe Keys │
          │  - Glossários │  │               │  │                │
          │  - Billing    │  │               │  │                │
          └───────────────┘  └───────────────┘  └────────────────┘
                                      │
                   ┌──────────────────┼──────────────────────┐
                   │                  │                      │
          ┌────────▼──────┐  ┌───────▼───────┐  ┌──────────▼─────┐
          │  LLM APIs     │  │  BigQuery     │  │  Vertex AI     │
          │  (Externas)   │  │  (Cliente)    │  │  Embeddings    │
          │  via BYOK     │  │  Read-only    │  │                │
          │  - Claude     │  │  Sandbox      │  │  Schema →      │
          │  - Gemini     │  │               │  │  Vetores       │
          │  - OpenAI     │  │               │  │                │
          └───────────────┘  └───────────────┘  └────────────────┘
```

### 7.2 Bounded Contexts

| Contexto | Responsabilidade | Módulo | Tag |
|----------|-----------------|--------|-----|
| **Tenant Management** | CRUD de tenants, planos, billing, onboarding | `api/modules/tenant/` | [INFERENCE] |
| **Auth & Identity** | Autenticação, autorização, RBAC, sessions | `api/modules/auth/` | [INFERENCE] |
| **Schema Intelligence** | Schema mapping, glossário, embeddings, contexto | `api/modules/schema/` | [INFERENCE] |
| **Query Engine** | NL-to-SQL, validação, execução, cache | `api/modules/query/` | [INFERENCE] |
| **Visualization** | Gráficos automáticos, insights, export | `api/modules/viz/` ou `web/components/charts/` | [INFERENCE] |
| **Billing** | Stripe integration, planos, invoices | `api/modules/billing/` | [INFERENCE] |

### 7.3 Comunicação

| # | Aspecto | Decisão | Justificativa | Tag |
|---|---------|---------|--------------|-----|
| 1 | Web ↔ API | REST (HTTP/JSON) | Simplicidade, cacheável, OpenAPI auto-generated pelo FastAPI | [INFERENCE] |
| 2 | Sync vs Async | Síncrono para queries (usuário espera resposta em < 5s) | Latência target de < 5s permite sync. Async para queries longas via WebSocket/SSE (stream de tokens da LLM) | [INFERENCE] |
| 3 | Real-time | Server-Sent Events (SSE) para streaming da resposta do LLM | Mesmo padrão usado por ChatGPT. Usuário vê a resposta sendo construída token por token | [INFERENCE] |
| 4 | Internal communication | In-process (módulos no mesmo FastAPI). Sem message queue no MVP | Com monolito modular, comunicação inter-módulo é chamada de função, não HTTP/gRPC | [INFERENCE] |

### 7.4 Consistência de Dados

| # | Aspecto | Decisão | Tag |
|---|---------|---------|-----|
| 1 | Cloud SQL | Forte — ACID transactions para operações de tenant, billing, schema | [INFERENCE] |
| 2 | Firestore | Eventual — sessions e cache não precisam de consistência forte | [INFERENCE] |
| 3 | BigQuery (cliente) | Read-only — sem preocupação de consistência (Veezoozin não escreve) | [BRIEFING] |
| 4 | Cross-store | Sem saga/2PC no MVP. Se precisar de consistência cross-store (ex: criar tenant no Cloud SQL + Firestore), usar padrão de idempotent operations com compensation | [INFERENCE] |

### 7.5 Integrações Externas

| Sistema | Tipo | Protocolo | Criticidade | Tag |
|---------|------|-----------|------------|-----|
| **LLM APIs (Claude, Gemini, OpenAI)** | NL-to-SQL core | HTTPS REST | Crítica — sem LLM não há produto | [BRIEFING] |
| **BigQuery (cliente)** | Dados source | BigQuery API (gRPC) | Crítica — sem dados não há resposta | [BRIEFING] |
| **Vertex AI Embeddings** | Schema intelligence | Vertex AI API | Alta — onboarding depende | [BRIEFING] |
| **Stripe** | Billing | HTTPS REST + Webhooks | Alta — sem billing não há receita | [INFERENCE] |
| **Firebase Auth** | Autenticação | Firebase SDK | Alta — sem auth não há acesso | [INFERENCE] |
| **MCP (Model Context Protocol)** | Conhecimento externo | MCP protocol | Baixa (Fase 2) | [BRIEFING] |

### 7.6 Observabilidade

| Camada | Ferramenta | Detalhe | Tag |
|--------|-----------|---------|-----|
| **Métricas** | Cloud Monitoring | Latência p50/p95/p99, error rate, throughput, custom metrics (queries/tenant) | [INFERENCE] |
| **Logs** | Cloud Logging | Structured logs (JSON) com campos: tenant_id, user_id, query_hash, latency, status | [INFERENCE] |
| **Traces** | Cloud Trace | Trace de request end-to-end: web → api → LLM → BigQuery → response | [INFERENCE] |
| **Alertas** | Cloud Monitoring Alerting | P1: uptime < 99%, error rate > 5%. P2: latência p95 > 10s. P3: custo diário > threshold | [INFERENCE] |
| **Dashboard** | Cloud Monitoring Dashboard | Painel operacional: health, latência, queries/min, erros, custo GCP | [INFERENCE] |

**Métricas custom necessárias:**

| Métrica | Por quê |
|---------|---------|
| `queries_per_tenant_per_day` | Detectar abuso / rate limiting |
| `llm_latency_ms` | Isoldar latência do LLM vs BigQuery |
| `bigquery_bytes_processed` | Controlar custo por tenant |
| `query_accuracy_feedback` | Medir KR de 85% precisão |
| `cache_hit_rate` | Otimizar latência e custo |

### 7.7 CI/CD e Deploy

| Aspecto | Decisão | Tag |
|---------|---------|-----|
| **Repositório** | Monorepo GitHub (web + api no mesmo repo) | [INFERENCE] |
| **CI** | GitHub Actions: lint → type-check → unit tests → integration tests → build | [INFERENCE] |
| **CD** | GitHub Actions → build Docker image → push GCR → deploy Cloud Run | [INFERENCE] |
| **Ambientes** | dev (local), staging (Cloud Run tag), production (Cloud Run) | [INFERENCE] |
| **Deploy strategy** | Rolling update (Cloud Run default). Zero-downtime. Rollback via revisão anterior | [INFERENCE] |
| **IaC** | Terraform para toda infra GCP (Cloud Run, Cloud SQL, Firestore, IAM, secrets) | [INFERENCE] |
| **Feature flags** | Firestore document com flags por tenant. Sem lib externa no MVP | [INFERENCE] |

**Pipeline CI/CD:**

```
push to main
  ↓
GitHub Actions trigger
  ↓
├── Lint (ruff/eslint)
├── Type Check (mypy/tsc)
├── Unit Tests (pytest/jest)
  ↓
Integration Tests (test containers: Cloud SQL, Firestore emulator)
  ↓
├── Build Docker (api)
├── Build Docker (web)
  ↓
Push to GCR (Google Container Registry)
  ↓
Deploy to Cloud Run (staging)
  ↓
Smoke Tests (staging)
  ↓
Manual promotion to production (tag-based)
```

### 7.8 Disaster Recovery

| Aspecto | RPO | RTO | Estratégia | Tag |
|---------|-----|-----|-----------|-----|
| **Cloud SQL** | < 1 hora | < 4 horas | Automated backups (diário) + point-in-time recovery | [INFERENCE] |
| **Firestore** | ~0 (sync) | < 1 hora | Managed service — GCP garante durabilidade | [INFERENCE] |
| **Cloud Run** | N/A (stateless) | < 5 min | Redeploy automático ou rollback de revisão | [INFERENCE] |
| **Secret Manager** | ~0 (replicated) | < 5 min | Managed service — GCP garante | [INFERENCE] |
| **BigQuery (cliente)** | N/A | N/A | Responsabilidade do cliente — Veezoozin é read-only | [BRIEFING] |

**Cenário de disaster:**
- **Cloud Run down:** Auto-restart. Se região inteira down: escalar para us-central1 (requer IaC multi-região, Fase 3).
- **Cloud SQL corrompido:** Restore do backup automatizado. RPO < 24h, RTO < 4h.
- **Credenciais comprometidas:** Rotação imediata via Secret Manager + revogação de sessions no Firebase Auth.

### 7.9 Estratégia de Testes

| Tipo | Ferramenta | Cobertura target | Tag |
|------|-----------|-----------------|-----|
| **Unit** | pytest (API), Jest (web) | > 70% de coverage | [INFERENCE] |
| **Integration** | pytest + testcontainers (Cloud SQL, Firestore emulator) | Todos os endpoints da API | [INFERENCE] |
| **Multi-tenant isolation** | pytest (custom suite) | TODOS os endpoints — verificar que Tenant A ≠ Tenant B | [INFERENCE] |
| **NL-to-SQL accuracy** | pytest + dataset de queries referência | > 85% de precisão (KR do bloco 1.3) | [INFERENCE] |
| **E2E** | Playwright (web) | Happy paths: signup → query → chart | [INFERENCE] |
| **Security** | OWASP ZAP (scanner) + review manual | OWASP Top 10 | [INFERENCE] |

**Testes de isolamento multi-tenant (obrigatório — recomendação do bloco 1.6):**

```python
# Pseudo-código
def test_tenant_isolation():
    # Setup: criar 2 tenants com dados diferentes
    tenant_a = create_tenant("A", data=[...])
    tenant_b = create_tenant("B", data=[...])

    # Test: autenticado como Tenant A, listar dados
    response = api.get("/queries", auth=tenant_a.token)
    assert all(r.tenant_id == "A" for r in response.data)
    assert not any(r.tenant_id == "B" for r in response.data)

    # Test: autenticado como Tenant A, tentar acessar dados de Tenant B
    response = api.get(f"/tenants/{tenant_b.id}/data", auth=tenant_a.token)
    assert response.status_code == 403
```

---

## 2. Análise do Especialista

### Avaliação da arquitetura

**Veredicto: ADEQUADA — monolito modular é a decisão correta para o contexto.**

| Critério | Avaliação |
|----------|-----------|
| Alinhamento com equipe (1 dev) | Excelente — monolito modular elimina overhead de microsserviços |
| Alinhamento com prazo (16 semanas) | Bom — 2 services (web + api) é gerenciável |
| Alinhamento com escala target (50 tenants) | Bom — Cloud Run auto-scale é suficiente para 50 tenants |
| Alinhamento com segurança (LGPD) | Bom — isolamento via middleware + testes automatizados |
| Alinhamento com custo (pay-per-use) | Excelente — Cloud Run scale-to-zero, zero custo sem tráfego |
| Path de evolução | Bom — bounded contexts permitem extração para microsserviços na Fase 3 se necessário |

### Riscos arquiteturais

| # | Risco | Probabilidade | Impacto | Tag |
|---|-------|-------------|---------|-----|
| 1 | **LLM API downtime** — Se Claude/Gemini ficam offline, produto inteiro para | Alta (10-15% chance/mês) | Crítico | [INFERENCE] |
| 2 | **Latência variável do LLM** — Latência de APIs de LLM varia de 500ms a 15s. Target de 5s pode ser violado frequentemente | Alta | Médio | [INFERENCE] |
| 3 | **Monorepo contention** — Com Claude Code gerando código, conflitos no monorepo podem ser frequentes | Baixa | Baixo | [INFERENCE] |

### Mitigação — Risco 1 (LLM API downtime)

| Campo | Detalhe |
|-------|---------|
| **Ação concreta** | Implementar fallback chain: se Claude falha → tentar Gemini → tentar OpenAI → retornar erro gracioso. Tenant configura ordem de preferência. BYOK multi-provider é enabler natural |
| **Responsável** | Fabio |
| **Custo estimado** | ~3 dias de desenvolvimento |
| **Prazo** | Sprint 4 (junto com BYOK multi-provider) |
| **Consequência se não resolver** | Single point of failure no LLM = produto down quando API externa falha = SLA violado |

### Mitigação — Risco 2 (Latência variável do LLM)

| Campo | Detalhe |
|-------|---------|
| **Ação concreta** | (1) Cache de queries similares no Firestore (hash da pergunta normalizada → SQL gerado); (2) SSE streaming para que usuário veja resposta sendo construída (percepção de velocidade); (3) Otimizar prompts para respostas menores; (4) Dashboard de latência por provider para tenant escolher o mais rápido |
| **Responsável** | Fabio |
| **Custo estimado** | Cache: 2 dias. SSE: 3 dias. Prompt optimization: contínuo |
| **Prazo** | Sprint 3-4 |
| **Consequência se não resolver** | Usuário espera > 10s em queries complexas → experiência ruim → churn |

---

## 3. Recomendações do Arquiteto

### R1 — Monolito modular com 2 Cloud Run services

**Proposta:** Manter arquitetura de 2 services: `veezoozin-web` (Next.js) e `veezoozin-api` (FastAPI). Monorepo GitHub. Bounded contexts como módulos Python no backend.

**Justificativa:** Simplicidade máxima para 1 dev. Escala independente. Path de evolução para microsserviços se necessário.

### R2 — SSE (Server-Sent Events) para streaming de respostas do LLM

**Proposta:** Ao receber pergunta, API inicia SSE stream. Frontend exibe resposta token por token (como ChatGPT). Usuário percebe velocidade mesmo que total seja 5-8s.

**Justificativa:** UX de streaming é vastamente superior a loading spinner de 5s. Padrão de mercado em apps com LLM.

### R3 — Cache de queries para reduzir latência e custo

**Proposta:** Cache em Firestore: hash(normalizar(pergunta) + tenant_id + schema_version) → SQL gerado + resultado. TTL de 24h. Cache hit = resposta em < 500ms sem chamar LLM nem BigQuery.

**Justificativa:** Muitas queries serão repetidas (mesma pergunta todo dia). Cache reduz latência (< 500ms vs 3-8s), custo de LLM (BYOK — beneficia o tenant) e custo de BigQuery.

### R4 — Fallback chain multi-provider para LLM

**Proposta:** BYOK com múltiplos providers. Tenant configura ordem de preferência: ex: [Claude, Gemini, OpenAI]. Se provider #1 falha (timeout ou error), tenta #2. Se todos falham, retorna erro gracioso.

**Justificativa:** Resiliência. Nenhuma API de LLM tem 100% uptime. Com BYOK multi-provider, o Veezoozin é resiliente a downtime de qualquer provedor individual.

### R5 — Investir em testes de precisão de NL-to-SQL como CI gate

**Proposta:** Dataset de referência com 100+ pares (pergunta, SQL esperado, resultado esperado). Rodar como test suite no CI. Gate: se precisão < 80%, build falha.

**Justificativa:** Precisão > 85% é KR do OKR 1 (bloco 1.3). Sem teste automatizado de precisão, regressões silenciosas degradam o KR principal.

**Ação:** Construir dataset de referência incrementalmente durante desenvolvimento
**Responsável:** Fabio
**Prazo:** Sprint 3 (junto com NL-to-SQL engine)
**Custo:** ~2 dias de setup + crescimento contínuo do dataset
**Consequência se não resolver:** Precisão degrada silenciosamente → usuários perdem confiança → churn

---

## Fontes

- [BRIEFING] `setup/briefing.md` — seções 5, 6, 10
- [BRIEFING] Blocos 1.1-1.6 — visão, personas, OKRs, organização, stack, privacidade
- [RAG] `saas-discovery-blueprint.md` — Componente 2 (Tenancy e Infraestrutura)
- [RAG] `ai-ml-discovery-blueprint.md` — Componente 2 (Desenvolvimento de Modelos), Componente 3 (MLOps e Serving)
- [INFERENCE] 30 inferências — arquitetura, CI/CD, DR, testes, observabilidade

**Contagem de tags:** [BRIEFING] 8 / [RAG] 4 / [INFERENCE] 30
