# Templates

Overrides de templates do pipeline. Arquivos aqui substituem os defaults de `dtg-artifacts/templates/`.

## Customization

A subpasta `customization/` contém os overrides mais comuns — scoring, iteração, estrutura do report e layout do HTML.
