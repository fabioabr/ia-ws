# Logos

Logo files in dark and light variants, available as PNG images and base64-encoded text for embedding in HTML reports.

## Contents

- `dark.png` — Dark variant logo image
- `dark-base64.txt` — Dark variant as base64 string
- `light.png` — Light variant logo image
- `light-base64.txt` — Light variant as base64 string
