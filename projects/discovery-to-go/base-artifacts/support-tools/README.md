# Support Tools

External tools that skills and agents can invoke. Each subfolder contains a standalone tool with its own source code and dependencies.

## Available Tools

| Tool | Description |
|------|-------------|
| `md-validator` | Python CLI that validates markdown files against workspace conventions |
