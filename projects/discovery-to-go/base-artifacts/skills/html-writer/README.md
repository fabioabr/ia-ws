# html-writer

Converts markdown documents into self-contained HTML reports following the workspace Design System. The generated HTML works by opening directly in a browser with no server required, and includes dark/light theme support with inline logos and styles.

This folder contains a single `SKILL.md` that defines the skill.
