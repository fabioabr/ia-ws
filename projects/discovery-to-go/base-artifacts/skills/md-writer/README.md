# md-writer

Markdown formatter that produces polished `.md` files following workspace conventions. Three modes of operation: format (reformat an existing file), create (generate a new file from raw content like notes or transcriptions), and polish (fix acronyms, frontmatter, headings, and emojis in a nearly-finished file).

This folder contains a single `SKILL.md` that defines the skill.
