# chart-specialist

Data visualization consultant for reports. Analyzes sample data and recommends the best visual format -- does not generate code, only advises which visualization to use.

Priority order: HTML/CSS (bars, cards, progress bars, timelines), Chart.js (radar, pie, line, scatter), informational card (narrative fallback).

This folder contains a single `SKILL.md` that defines the skill.
